{
  "name": "zeptoclaw-router",
  "version": "1.1.0",
  "description": "ZeptoClaw router — single-model NVIDIA inkling router (openclaw-compatible, no openclaw dependency)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "^5.0.0",
    "wrangler": "^3.0.0"
  }
}
